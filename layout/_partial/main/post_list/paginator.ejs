<% if ((is_home() || is_category() || is_tag() || page.__xs_index_blog) && page.total > 1) { %>
  <div class='paginator-wrap dis-select<%- scrollreveal(' ') %>'>
  <%- paginator({
    prev_text: '',
    next_text: '',
    force_prev_next:true
  }) %>
  </div>
<% } %>
